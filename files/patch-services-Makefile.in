--- services/Makefile.in.orig	2011-08-30 15:19:55.000000000 +0300
+++ services/Makefile.in	2011-08-30 15:20:04.000000000 +0300
@@ -647,8 +647,6 @@
 
 
 install-exec-local:
-	mkdir -p $(DESTDIR)/etc/vmware-tools
-	ln -sf `dirname $(VMSVC_PLUGIN_INSTALLDIR)` $(DESTDIR)/etc/vmware-tools/plugins
 
 # Tell versions [3.59,3.63) of GNU make to not export all variables.
 # Otherwise a system limit (for SysV at least) may be exceeded.
